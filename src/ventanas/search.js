import React, {Component} from 'react';
import {StyleSheet, View, Text} from 'react-native';

class Search extends Component{
    render(){
        return(
            <Text>Realizar búsquedas</Text>
        )
    }
}

const styles = StyleSheet.create({})

export default Search;